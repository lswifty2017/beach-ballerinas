To Do:

* Render correct images, compression, caching etc
* Image alt tags
* Figure out css transition group
* Add tests to header-nav
* Add tests and linter to pipeline
* Add navbar state for active page
* Add schemas to graphql for null values

Bugs:

*

      - {
        label: Term 2,
        name: term-two,
        widget: list,
        fields:
          - {
              label: Start Date,
              name: start-date,
              widget: datetime,
              format: "DD.MM.YYYY",
              dateFormat: "DD.MM.YYYY",
              timeFormat: false,
            }
          - {
              label: End Date,
              name: end-date,
              widget: datetime,
              format: "DD.MM.YYYY",
              dateFormat: "DD.MM.YYYY",
              timeFormat: false,
            }
        }
      - {
        label: Term 3,
        name: term-three,
        widget: list,
        fields:
          - {
              label: Start Date,
              name: start-date,
              widget: datetime,
              format: "DD.MM.YYYY",
              dateFormat: "DD.MM.YYYY",
              timeFormat: false,
            }
          - {
              label: End Date,
              name: end-date,
              widget: datetime,
              format: "DD.MM.YYYY",
              dateFormat: "DD.MM.YYYY",
              timeFormat: false,
            }
        }
      - {
        label: Term 4,
        name: term-four,
        widget: list,
        fields:
          - {
            label: Start Date,
            name: start-date,
            widget: datetime,
            format: "DD.MM.YYYY",
            dateFormat: "DD.MM.YYYY",
            timeFormat: false,
          }
          - {
            label: End Date,
            name: end-date,
            widget: datetime,
            format: "DD.MM.YYYY",
            dateFormat: "DD.MM.YYYY",
            timeFormat: false,
          }
        }